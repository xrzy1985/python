import random
import sys
import os

# Lambda expressions are a way of creating a small
# function really quickly and concise

# func = lambda a, b, c : expression

add lambda x, y : x + y

print(add(1,2))

# OR

def func1 (a, b, c):
    value = (a + b) - c
    return value

print(func1(1,2,3))

# So, you can see how lambda expressions really help us out in the
# actual lines of code we need to write. 
